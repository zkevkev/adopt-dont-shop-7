<%= @application.name %>
<% @application.pets.each do |pet| %>
  <%= pet.name %>
  <%# having the url here may be completely unnecessary %>
  <%= button_to "Approve this Pet", "/admin/applications/#{@application.id}", method: :get, params: { approved: true } %>
<% end %>